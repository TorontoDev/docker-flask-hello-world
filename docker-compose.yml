version: '3.4'
services:
    flask-server:
        image: my-custom-flask-image
        build:
            context: .
            dockerfile: flask-dockerfile
        ports:
            - 5000:5000
        volumes:
            - .:/app:cached